<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- רקע לבן -->
    <solid android:color="#FFFFFF"/>

    <!-- פינות מעוגלות -->
    <corners android:radius="18dp"/>

    <!-- מסגרת עדינה -->
    <stroke
        android:width="1dp"
        android:color="#22000000"/>

    <!-- צל עדין (עובד בעיקר ב-API חדש) -->
    <padding
        android:left="6dp"
        android:top="6dp"
        android:right="6dp"
        android:bottom="6dp"/>
</shape>